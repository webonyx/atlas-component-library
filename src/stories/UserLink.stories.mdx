import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

import { UserLink }             from './UserLink';
import { Router }               from 'react-router-dom';
import { createBrowserHistory } from 'history';

<!--👇 The title prop determines where your story goes in the story list -->
<Meta title="UserLink" component={UserLink} decorators={[
  (Story) => (
    <Router history={createBrowserHistory()}>
      <Story/>
    </Router>
  ),
]}/>

<!--👇 We create a “template” of how args map to rendering -->
export const Template = (args) => <UserLink {...args} />;

<!-- 👇 The args you need here will depend on your component -->
## Args
<ArgsTable of={UserLink}/>

## Stories
#### Default
<Canvas>
  <Story
    name="Default"
    args={{
      children: null,
      user: { attuid: "jd1234", name: 'John Doe' },
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

#### Use Children
<Canvas>
  <Story
    name="RenderChildren"
    args={{
      children: (<div>This is my link</div>),
      user: { attuid: "jd1234", name: 'John Doe' },
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

#### Custom To Link
<Canvas>
  <Story
    name="CustomTo"
    args={{
      children: null,
      user: { attuid: "jd1234", name: 'John Doe' },
      to: '/cdo/people/:id'
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

#### Link an image
<Canvas>
  <Story
    name="ImageLink"
    args={{
      children: <img src="assets/profile-photo.png" alt="John Doe" />,
      user: { attuid: "jd1234", name: 'John Doe' },
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
